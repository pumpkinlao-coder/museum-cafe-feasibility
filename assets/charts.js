/* Generated by Trae Work */
(function () {
  var style = getComputedStyle(document.documentElement);
  var accent = style.getPropertyValue('--accent').trim();
  var accent2 = style.getPropertyValue('--accent2').trim();
  var ink = style.getPropertyValue('--ink').trim();
  var muted = style.getPropertyValue('--muted').trim();
  var rule = style.getPropertyValue('--rule').trim();
  var bg2 = style.getPropertyValue('--bg2').trim();
  var FONT = '"Work Sans","PingFang SC","Noto Sans CJK SC","WenQuanYi Micro Hei",sans-serif';
  var palette = [accent, accent2, muted, accent + '99', accent2 + '99', accent + '66'];
  var baseText = { fontFamily: FONT, color: ink };
  var baseAxis = { fontFamily: FONT, color: muted };

  function tooltip() {
    return { trigger: 'axis', appendToBody: true, backgroundColor: bg2, borderColor: rule, textStyle: { color: ink, fontFamily: FONT } };
  }
  function chart(id) {
    return echarts.init(document.getElementById(id), null, { renderer: 'svg' });
  }

  // ---- 图1 光明区消费类指标 ----
  (function () {
    var c = chart('chart-econ');
    c.setOption({
      animation: false,
      color: [accent, accent2],
      tooltip: tooltip(),
      legend: { data: ['2024年', '2025年'], textStyle: baseText, top: 0 },
      grid: { left: 60, right: 20, top: 48, bottom: 40 },
      xAxis: { type: 'category', data: ['社会消费品零售总额(亿元)', '住宿和餐饮业增加值(亿元)'], axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis },
      yAxis: { type: 'value', name: '亿元', nameTextStyle: baseAxis, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } } },
      series: [
        { name: '2024年', type: 'bar', barWidth: 36, data: [285.83, 25.16], itemStyle: { color: accent + 'aa' }, label: { show: true, position: 'top', color: muted, fontFamily: FONT, formatter: '{c}' } },
        { name: '2025年', type: 'bar', barWidth: 36, data: [330.53, 29.72], itemStyle: { color: accent2 }, label: { show: true, position: 'top', color: ink, fontFamily: FONT, formatter: '{c}' } }
      ]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图2 13号线客流 ----
  (function () {
    var c = chart('chart-traffic');
    c.setOption({
      animation: false,
      color: [accent],
      tooltip: tooltip(),
      grid: { left: 30, right: 60, top: 20, bottom: 30 },
      xAxis: { type: 'value', name: '万人次/日', nameTextStyle: baseAxis, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } } },
      yAxis: { type: 'category', data: ['二期北延段(光明段)', '光明区地铁全网', '13号线全线'], axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis },
      series: [{
        type: 'bar', barWidth: 30,
        data: [
          { value: 11.3, itemStyle: { color: accent + '77' } },
          { value: 38.6, itemStyle: { color: accent2 } },
          { value: 50.1, itemStyle: { color: accent } }
        ],
        label: { show: true, position: 'right', color: ink, fontFamily: FONT, formatter: '{c}' }
      }]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图3 五年租金成本 ----
  (function () {
    var c = chart('chart-rent5');
    c.setOption({
      animation: false,
      color: [accent, accent2, muted, accent + '77'],
      tooltip: tooltip(),
      grid: { left: 60, right: 20, top: 20, bottom: 40 },
      xAxis: { type: 'category', data: ['1F 7号(424.76㎡)', '2F 340㎡', '2F 203㎡', '6F(421.05㎡)'], axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis },
      yAxis: { type: 'value', name: '万元', nameTextStyle: baseAxis, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } } },
      series: [{
        type: 'bar', barWidth: 64,
        data: [
          { value: 255, itemStyle: { color: accent } },
          { value: 204, itemStyle: { color: accent2 } },
          { value: 122, itemStyle: { color: muted } },
          { value: 0, itemStyle: { color: accent + '77' } }
        ],
        label: { show: true, position: 'top', color: ink, fontFamily: FONT, formatter: function (p) { return p.value === 0 ? '0 元' : p.value + ' 万'; } }
      }]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图4 品类雷达 ----
  (function () {
    var c = chart('chart-radar');
    c.setOption({
      animation: false,
      color: palette,
      tooltip: Object.assign(tooltip(), { trigger: 'item' }),
      legend: { data: ['现制咖啡', '现制茶饮', '烘焙甜品', '轻食简餐', '文创联名餐饮'], textStyle: baseText, top: 0, type: 'scroll' },
      radar: {
        indicator: [
          { name: '毛利率', max: 100 },
          { name: '客群契合', max: 100 },
          { name: '市场机会', max: 100 },
          { name: '运营轻便', max: 100 },
          { name: '场景适配', max: 100 }
        ],
        radius: '62%',
        center: ['50%', '56%'],
        axisName: { color: ink, fontFamily: FONT, fontSize: 12 },
        splitLine: { lineStyle: { color: rule } },
        splitArea: { areaStyle: { color: [bg2, bg2] } },
        axisLine: { lineStyle: { color: rule } }
      },
      series: [{
        type: 'radar',
        data: [
          { name: '现制咖啡', value: [85, 95, 70, 80, 90], areaStyle: { color: accent + '33' }, lineStyle: { color: accent, width: 2 } },
          { name: '现制茶饮', value: [65, 80, 55, 85, 75], areaStyle: { color: accent2 + '33' }, lineStyle: { color: accent2, width: 2 } },
          { name: '烘焙甜品', value: [70, 75, 75, 70, 70], areaStyle: { color: muted + '33' }, lineStyle: { color: muted, width: 2 } },
          { name: '轻食简餐', value: [55, 70, 80, 50, 80], areaStyle: { color: accent + '22' }, lineStyle: { color: accent + '99', width: 2, type: 'dashed' } },
          { name: '文创联名餐饮', value: [60, 90, 65, 60, 85], areaStyle: { color: accent2 + '22' }, lineStyle: { color: accent2 + '99', width: 2, type: 'dashed' } }
        ]
      }]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图5 业态面积配置 ----
  (function () {
    var c = chart('chart-mix');
    c.setOption({
      animation: false,
      color: [accent, accent2, muted, accent + '66'],
      tooltip: Object.assign(tooltip(), { trigger: 'item', formatter: '{b}: {c}㎡ ({d}%)' }),
      legend: { textStyle: baseText, bottom: 0, type: 'scroll' },
      series: [{
        type: 'pie', radius: ['42%', '68%'], center: ['50%', '46%'],
        itemStyle: { borderColor: bg2, borderWidth: 2 },
        label: { color: ink, fontFamily: FONT, formatter: '{b}\n{d}%' },
        data: [
          { name: '咖啡烘焙主区', value: 190 },
          { name: '轻食简餐区', value: 126 },
          { name: '文创联名零售区', value: 63 },
          { name: '后厨与仓储', value: 42 }
        ]
      }]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图6 年固定成本构成 ----
  (function () {
    var c = chart('chart-cost');
    c.setOption({
      animation: false,
      color: [accent, accent2, muted, ink],
      tooltip: tooltip(),
      legend: { data: ['租金', '物业费', '人力', '水电燃气/杂费'], textStyle: baseText, top: 0 },
      grid: { left: 60, right: 20, top: 48, bottom: 40 },
      xAxis: { type: 'category', data: ['1F方案(424.76㎡)', '6F方案(421.05㎡)'], axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis },
      yAxis: { type: 'value', name: '万元/年', nameTextStyle: baseAxis, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } } },
      series: [
        { name: '租金', type: 'bar', stack: 'c', barWidth: 80, data: [51.0, 0], label: { show: true, color: ink, fontFamily: FONT, formatter: function (p) { return p.value === 0 ? '' : p.value + '万'; } } },
        { name: '物业费', type: 'bar', stack: 'c', data: [2.55, 2.53] },
        { name: '人力', type: 'bar', stack: 'c', data: [81.6, 81.6] },
        { name: '水电燃气/杂费', type: 'bar', stack: 'c', data: [12.7, 12.6], label: { show: true, position: 'top', color: ink, fontFamily: FONT, formatter: function (p) { return p.value + 0; } } }
      ]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图7 情景收入与盈亏平衡 ----
  (function () {
    var c = chart('chart-breakeven');
    c.setOption({
      animation: false,
      color: [accent],
      tooltip: tooltip(),
      legend: { data: ['情景年收入'], textStyle: baseText, top: 0 },
      grid: { left: 60, right: 20, top: 48, bottom: 40 },
      xAxis: { type: 'category', data: ['A 保守', 'B 中性', 'C 乐观'], axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis },
      yAxis: { type: 'value', name: '万元/年', nameTextStyle: baseAxis, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } } },
      series: [{
        name: '情景年收入', type: 'bar', barWidth: 64,
        data: [
          { value: 320, itemStyle: { color: accent + '88' } },
          { value: 560, itemStyle: { color: accent } },
          { value: 960, itemStyle: { color: accent } }
        ],
        label: { show: true, position: 'top', color: ink, fontFamily: FONT, formatter: '{c}' },
        markLine: {
          symbol: 'none',
          label: { color: ink, fontFamily: FONT, fontSize: 11, formatter: '{b}' },
          data: [
            { name: '1F盈亏平衡 217.4', yAxis: 217.4, lineStyle: { color: accent2, width: 2, type: 'dashed' }, label: { formatter: '1F盈亏平衡 217.4万', color: accent2 } },
            { name: '6F盈亏平衡 142.3', yAxis: 142.3, lineStyle: { color: muted, width: 2, type: 'dashed' }, label: { formatter: '6F盈亏平衡 142.3万', color: muted } }
          ]
        }
      }]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图8 净利润对比 ----
  (function () {
    var c = chart('chart-profit');
    c.setOption({
      animation: false,
      color: [accent, accent2],
      tooltip: tooltip(),
      legend: { data: ['6F方案', '1F方案'], textStyle: baseText, top: 0 },
      grid: { left: 60, right: 20, top: 48, bottom: 40 },
      xAxis: { type: 'category', data: ['A 保守(320万需求)', 'B 中性(560万需求)', 'C 乐观(960万需求)'], axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis },
      yAxis: { type: 'value', name: '万元/年', nameTextStyle: baseAxis, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } } },
      series: [
        { name: '6F方案', type: 'bar', barWidth: 34, data: [120.8, 202.2, 202.2], label: { show: true, position: 'top', color: ink, fontFamily: FONT, formatter: '{c}' } },
        { name: '1F方案', type: 'bar', barWidth: 34, data: [69.7, 154.9, 154.9], label: { show: true, position: 'top', color: muted, fontFamily: FONT, formatter: '{c}' } }
      ]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图9 月度保本 ----
  (function () {
    var c = chart('chart-monthly');
    c.setOption({
      animation: false,
      color: [accent],
      tooltip: tooltip(),
      legend: { data: ['情景月营收'], textStyle: baseText, top: 0 },
      grid: { left: 60, right: 20, top: 48, bottom: 40 },
      xAxis: { type: 'category', data: ['A 保守', 'B 中性', 'C 乐观'], axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis },
      yAxis: { type: 'value', name: '万元/月', nameTextStyle: baseAxis, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } } },
      series: [{
        name: '情景月营收', type: 'bar', barWidth: 64,
        data: [
          { value: 26.7, itemStyle: { color: accent + '88' } },
          { value: 36.6, itemStyle: { color: accent } },
          { value: 36.6, itemStyle: { color: accent } }
        ],
        label: { show: true, position: 'top', color: ink, fontFamily: FONT, formatter: '{c}' },
        markLine: {
          symbol: 'none',
          label: { color: ink, fontFamily: FONT, fontSize: 11 },
          data: [
            { name: '保本线 11.86万/月', yAxis: 11.86, lineStyle: { color: accent2, width: 2, type: 'dashed' }, label: { formatter: '保本线 11.86万/月', color: accent2 } }
          ]
        }
      }]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图10 6F三档面积-净利润 ----
  (function () {
    var c = chart('chart-area-profit');
    c.setOption({
      animation: false,
      color: [accent2, accent, muted],
      tooltip: tooltip(),
      legend: { data: ['保守情景(5%转化)', '中性情景(8%转化)', '乐观情景(12%转化)'], textStyle: baseText, top: 0 },
      grid: { left: 60, right: 30, top: 48, bottom: 40 },
      xAxis: { type: 'category', boundaryGap: false, data: ['109.55㎡', '395.35㎡', '421.05㎡'], axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis },
      yAxis: { type: 'value', name: '年净利润(万元)', nameTextStyle: baseAxis, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } } },
      series: [
        { name: '保守情景(5%转化)', type: 'line', smooth: true, symbolSize: 8, data: [46.9, 128.6, 120.8], lineStyle: { width: 3 }, label: { show: true, position: 'top', color: muted, fontFamily: FONT, formatter: '{c}' } },
        { name: '中性情景(8%转化)', type: 'line', smooth: true, symbolSize: 8, data: [46.9, 192.8, 202.2], lineStyle: { width: 3 }, areaStyle: { color: accent + '22' }, label: { show: true, position: 'top', color: ink, fontFamily: FONT, formatter: '{c}' }, markPoint: {
          symbol: 'pin', symbolSize: 46,
          data: [
            { coord: ['421.05㎡', 202.2], label: { formatter: '推荐421.05㎡', color: '#F3EFE4', fontFamily: FONT, fontSize: 10 } }
          ]
        } },
        { name: '乐观情景(12%转化)', type: 'line', smooth: true, symbolSize: 8, data: [46.9, 192.8, 202.2], lineStyle: { width: 2, type: 'dashed' } }
      ]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图11 座位与人员 ----
  (function () {
    var c = chart('chart-seats-staff');
    c.setOption({
      animation: false,
      color: [accent, accent2],
      tooltip: tooltip(),
      legend: { data: ['座位数', '人员配置'], textStyle: baseText, top: 0 },
      grid: { left: 60, right: 60, top: 48, bottom: 40 },
      xAxis: { type: 'category', data: ['109.55㎡', '395.35㎡', '421.05㎡'], axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis },
      yAxis: [
        { type: 'value', name: '座位数', nameTextStyle: baseAxis, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } } },
        { type: 'value', name: '人员', nameTextStyle: baseAxis, axisLabel: baseAxis, splitLine: { show: false } }
      ],
      series: [
        { name: '座位数', type: 'bar', barWidth: 40, data: [41, 148, 157], label: { show: true, position: 'top', color: ink, fontFamily: FONT, formatter: '{c}座' } },
        { name: '人员配置', type: 'line', yAxisIndex: 1, smooth: true, symbolSize: 8, data: [4, 11, 12], lineStyle: { color: accent2, width: 3 }, label: { show: true, position: 'top', color: accent2, fontFamily: FONT, formatter: '{c}人' } }
      ]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图12 菜单工程矩阵 ----
  (function () {
    var c = chart('chart-menu');
    var data = [
      { name: '拿铁', value: [92, 72, 100] },
      { name: '美式', value: [88, 78, 120] },
      { name: '巴斯克', value: [80, 70, 60] },
      { name: '黄油可颂', value: [75, 68, 70] },
      { name: '轻食沙拉', value: [70, 58, 80] },
      { name: '厚蛋吐司', value: [68, 55, 60] },
      { name: '番茄意面', value: [65, 60, 55] },
      { name: '现制茶饮', value: [40, 50, 35] },
      { name: '手冲单品', value: [35, 80, 15] },
      { name: '展览主题特调', value: [30, 75, 20] },
      { name: '文创蛋糕', value: [28, 72, 18] },
      { name: '冷饮旧款', value: [25, 45, 10] }
    ];
    c.setOption({
      animation: false,
      tooltip: Object.assign(tooltip(), { trigger: 'item', formatter: function (p) { return p.name + '<br>人气: ' + p.value[0] + ' | 毛利率: ' + p.value[1] + '%'; } }),
      grid: { left: 60, right: 40, top: 40, bottom: 50 },
      xAxis: {
        type: 'value', name: '人气(点单频次)', min: 0, max: 100, nameTextStyle: baseAxis,
        axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } }
      },
      yAxis: {
        type: 'value', name: '毛利率(%)', min: 30, max: 90, nameTextStyle: baseAxis,
        axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } }
      },
      graphic: [
        { type: 'line', left: '50%', top: 40, bottom: 50, style: { stroke: accent2, lineDash: [4, 4], lineWidth: 1 } },
        { type: 'line', left: 60, right: 40, top: '50%', style: { stroke: accent2, lineDash: [4, 4], lineWidth: 1 } },
        { type: 'text', left: '61%', top: 44, style: { text: '明星品', fill: accent, font: '600 12px "Work Sans"' } },
        { type: 'text', left: '8%', top: 44, style: { text: '问题品', fill: accent2, font: '600 12px "Work Sans"' } },
        { type: 'text', left: '61%', bottom: 52, style: { text: '金牛品', fill: muted, font: '600 12px "Work Sans"' } },
        { type: 'text', left: '8%', bottom: 52, style: { text: '瘦狗品', fill: muted, font: '600 12px "Work Sans"' } }
      ],
      series: [{
        type: 'scatter', symbolSize: function (d) { return Math.max(14, Math.sqrt(d[2]) * 3.2); },
        label: { show: true, position: 'top', color: ink, fontFamily: FONT, fontSize: 11, formatter: '{b}' },
        itemStyle: { color: function (p) {
          var x = p.value[0], y = p.value[1];
          if (x >= 50 && y >= 65) { return accent; }
          if (x >= 50 && y < 65) { return muted; }
          if (x < 50 && y >= 65) { return accent2; }
          return '#8A8A8A';
        }, opacity: 0.85 },
        data: data
      }]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();

  // ---- 图13 2号位与3号位对比 ----
  (function () {
    var c = chart('chart-stall');
    if (!c) { return; }
    c.setOption({
      animation: false,
      color: [accent, accent2, muted],
      tooltip: tooltip(),
      legend: { data: ['保本月营收(万元)', '初始投入(万元)'], textStyle: baseText, top: 0 },
      grid: { left: 60, right: 20, top: 48, bottom: 40 },
      xAxis: { type: 'category', data: ['2号位 艺术甜品(54㎡)', '3号位 时尚餐品(64.8㎡)'], axisLine: { lineStyle: { color: rule } }, axisLabel: baseAxis },
      yAxis: { type: 'value', name: '万元', nameTextStyle: baseAxis, axisLabel: baseAxis, splitLine: { lineStyle: { color: rule } } },
      series: [
        { name: '保本月营收(万元)', type: 'bar', barWidth: 36, data: [3.53, 3.74], label: { show: true, position: 'top', color: ink, fontFamily: FONT, formatter: '{c}万/月' } },
        { name: '初始投入(万元)', type: 'bar', barWidth: 36, data: [33.5, 39.8], label: { show: true, position: 'top', color: muted, fontFamily: FONT, formatter: '{c}万' } }
      ]
    });
    window.addEventListener('resize', function () { c.resize(); });
  })();
})();
